float foo (float a, float b) {
	float result;
	if (a > b) {
		result = a / (b - 2);	
	} else {
		result = a + b;
	}	
	return result;
}
//ASSERT((return >= 0) ^ (return <= 1)


//ASSERT(return * return > (a + b)/2)






//(return = (n*(n + 1))/2)
//(return >= 0) ^ (return <= 1)


//pri //ASSERT(a + 0.375 = 0)^(b = 0.5)
