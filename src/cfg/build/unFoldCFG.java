package cfg.build;

import cfg.node.CFGNode;

/*
 * @va
 * test teo thoi
 */
public class unFoldCFG {
	ControlFlowGraph unFold(ControlFlowGraph cfg, int nloop) {
		CFGNode iter = cfg.getStart();
		
		return cfg;
	}
	
}
